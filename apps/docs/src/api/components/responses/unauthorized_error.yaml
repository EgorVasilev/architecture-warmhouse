description: Не авторизован
content:
  application/json:
    schema:
      $ref: '../schemas/error.yaml'
    example:
      error:
        code: "UNAUTHORIZED"
        message: "Требуется аутентификация" 